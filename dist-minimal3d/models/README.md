# Models Folder

Place your .glb or .gltf 3D model files here.

Update the path in `js/core/main.js` to match your file name, e.g., 'models/my-model.glb'.

The app will load this model automatically on startup.